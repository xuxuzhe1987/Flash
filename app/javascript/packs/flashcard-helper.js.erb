
function flashCard(question, answer, hint) {
  this.question = question;
  this.answer = answer;
  this.hint = hint;
}
// const newDeck = <%= @deck. =>;
const newCard;
const front = document.getElementById("front");
const back = document.getElementById("back");
const flip = document.getElementById("flip");
const hint = document.getElementById("hint")
// var submit = document.getElementById("submit");
// var clearDeck = document.getElementById("clearDeck");
// const formFront, formBack;

// function slideIn(){
//   $('#importExport').animate({'left':'10px'},500);

//     };
// function slideOut(){
//         $('#importExport').animate({'left':'-610px'},500);
//     };
let card1 = new flashCard(
  "Lexical Environment",
  "Where code sits in relation to any surrounding code",
  "General"
);
let card2 = new flashCard(
  "Execution Context",
  "How, Why, When, and Where code is executed",
  "General"
);
let card3 = new flashCard(
  "JSON",
  "JavaScript Object Notation, for storing objects and their enclosed data. Often referred to as Key Value Pairs",
  "Objects"
);

const myCards = [card1, card2, card3];
const cardIndex = 0;

front.innerHTML = card1.question;
back.innerHTML = card1.answer;
back.style.visibility = "hidden";

function flashView() {
  if (front.style.visibility != "hidden") {
    front.style.visibility = "hidden";
    back.style.visibility = "visible";
  } else {
    front.style.visibility = "visible";
    back.style.visibility = "hidden";
  }
}

function hintView(){
  if ()
}

function nextCard() {
  cardIndex = (cardIndex + 1) % myCards.length;
  front.innerHTML = myCards[cardIndex].question;
  back.innerHTML = myCards[cardIndex].answer;
}
